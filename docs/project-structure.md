# Project Structure

This project is a **monolith**.

**Root Path:** `/home/tiziano_di_gennaro/git/jfrog2nexus`
**Primary Technology:** Rust
**Frameworks:** Tokio, Reqwest, Clap, Axum, SQLx
**Description:** A monolithic Rust application serving primarily as a CLI tool (`jfrog2nexus`), but also incorporating backend-like features (API endpoints with `axum`, database interactions with `sqlx`).
